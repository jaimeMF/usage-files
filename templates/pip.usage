INSTALLED_PACKAGES = !@pip@ list | sed "s/ \((.*)\)//g";
@pip@ install [-U | --upgrade | --force-reinstall] ...;
@pip@ uninstall [--yes] <INSTALLED_PACKAGES>;
@pip@ list [--outdated | --uptodate | --editable | --local] ...;
@pip@ show <INSTALLED_PACKAGES> ...;
@pip@ freeze;
@pip@ --version;
